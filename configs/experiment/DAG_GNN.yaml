# @package _global_

# to execute this experiment run:
# python train.py experiment=DAG_GNN
defaults:
  - override /datamodule: sachs.yaml
  - override /model: DAG_GNN.yaml
  - override /loop: DAG_GCN_loop.yaml

tags: ["sachs", "DAG_GCN"]