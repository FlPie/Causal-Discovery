# @package _global_

# to execute this experiment run:
# python train.py experiment=GAE
defaults:
  - override /datamodule: sachs.yaml
  - override /model: GAE.yaml
  - override /loop: GAE_loop.yaml

tags: ["sachs", "GAE"]